pipeline {
agent any 
stages {
       stage('---package---') {
       steps {
       echo "mvn package"
              echo "mvn clean"
       }
       }  stage('---clean---') {
       steps {
   
              echo "mvn clean"
       }
       }
       }
}
