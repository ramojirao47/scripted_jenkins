pipeline {
agent any 
stages {
       stage('---package---') {
       steps {
       echo "mvn package"
              echo "mvn clean"
       }
       }  stage('---package---') {
       steps {
   
              echo "mvn clean"
       }
       }
       }
}
