pipeline {
agent any 
stages {
       stage('---package---') {
       steps {
       echo "mvn package"
             
       }
       }  
       stage('---clean---') {
       steps {
   
              echo "mvn clean"
       }
       }
       }
}
